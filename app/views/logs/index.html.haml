%h1 Listing logs

= render 'chart'

%table.table
  %thead
    %tr
      %th Visits
      %th Uri
      %th graph

  %tbody
    - @logs.each do |log|
      %tr
        %td= log.visits
        %td= link_to log.uri, log.uri
        %th
          .progress
            .progress-bar.progress-bar-info.progress-bar-striped{role: :progressbar, 'aria-valuenow' => 50, 'aria-valuemin' => 0, 'aria-valuemax' => 100 }
              %span.sr-only
                20% Complete